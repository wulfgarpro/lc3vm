# l3cvm

A simple LC-3 VM.

## Requirements

- A C compiler.
- CMake >= 3.29.
- A CMake generator, e.g. Make, Ninja, etc.

## Build & Run

```shell
mkdir build
cd build
cmake ../
cmake --build .
./vm
```
